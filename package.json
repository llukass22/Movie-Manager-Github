{
  "name": "movie-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Personal movie watchlist with IMDb search and PWA support",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "build": "npm run check",
    "check": "node --check server.js && node --check app.js && node --check service-worker.js"
  },
  "engines": {
    "node": ">=22"
  }
}
